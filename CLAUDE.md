# تعليمات الوكلاء — مشروع bot

> يُقرأ هذا الملف تلقائياً من Claude Code. النسخة المقابلة لـ Codex هي
> `AGENTS.md`، وكلاهما يشير إلى نفس المرجعين أدناه.

## اقرأ هذين أولاً — قبل أي تعديل

| الملف | ماذا يحوي |
|---|---|
| **`docs/DISCUSSION.md`** | سجل النقاش مع المالك: القرارات و**لماذا** اتُّخذت، والأرقام |
| **`docs/ARCHITECTURE.md`** | العقد المُلزِم: البنية، الواجهات، تقسيم الملكية |

لا تبدأ العمل قبل قراءتهما. كثير من الاقتراحات البديهية سبق أن نوقشت واستُبعدت
لأسباب مكتوبة هناك.

## الخلاصة في ستة أسطر

1. **الموقع هو المنتج. البوت قناة جلب زبائن** — البوت لا يعالج صوراً.
2. **الترميم بنماذج AI** — فلاتر Canvas لا تبلغ نتيجة Remini. الشعار القديم
   «بدون ذكاء اصطناعي» أُلغي.
3. **لا نظام دفع** في هذه المرحلة، ولا نجوم تلگرام.
4. **سقف الدقة 2048px إلزامي** قبل أي نداء للمزوّد — بدونه الفاتورة تتضاعف 15 مرة.
5. **كل نداء للنماذج عبر `lib/enhance/provider.ts`** — لا استدعاء مباشر.
6. **كل مسار يعالج صوراً يمر على `lib/ratelimit.ts`** — صمّام أمان الفاتورة.

## نطاقك

راجع `docs/ARCHITECTURE.md §6` لتقسيم الملكية. لا تعدّل ملفاً خارج نطاقك بدون
تنسيق عبر Issue أو PR.

## المشاركة في النقاش

النقاش المفتوح يجري في GitHub Issues. الوكلاء لا يتواصلون مباشرة — الـ Issue هو
مكان تبادل الآراء.

عند إبداء رأيك في قرار معماري:

- **اذكر الملف والسطر** الذي يستند إليه رأيك.
- **اعترض بالأرقام** إذا اعترضت على تقدير تكلفة، لا بالانطباع.
- **إذا خالفت قراراً في `DISCUSSION.md`** فاذكر رقم القسم صراحةً واشرح ما تغيّر.
- القرارات المستبعَدة في `DISCUSSION.md §5` مغلقة بقرار المالك — لا تُعاد.

## قواعد العمل

- `npm run lint && npm run build` يجب أن يمرّا نظيفين قبل أي دفع.
- لا تُسكِت خطأً لتمرّ البناء — أصلح السبب.
- نصوص المستخدم بالعربية، تعليقات الكود بالإنجليزية.
- الأسرار لا تدخل الگت. `.env.example` يوثّق الأسماء فقط.

## أوامر المشروع

```bash
npm install
npm run dev     # vinext dev
npm run build   # vinext build
npm run lint    # eslint
```
