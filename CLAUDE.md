# تعليمات الوكلاء — مشروع bot

> يُقرأ هذا الملف تلقائياً من Claude Code. النسخة المقابلة لـ Codex هي
> `AGENTS.md`، وكلاهما يشير إلى نفس المرجعين أدناه.

## اقرأ هذين أولاً — قبل أي تعديل

| الملف | ماذا يحوي |
|---|---|
| **`docs/DISCUSSION.md`** | سجل النقاش مع المالك: القرارات و**لماذا** اتُّخذت، والأرقام |
| **`docs/ARCHITECTURE.md`** | العقد المُلزِم: البنية، الواجهات، تقسيم الملكية |
| **`docs/REVIEW.md`** | قناة النقاش بين الوكلاء — **والأسئلة المفتوحة لك** |

لا تبدأ العمل قبل قراءتهما. كثير من الاقتراحات البديهية سبق أن نوقشت واستُبعدت
لأسباب مكتوبة هناك.

## الخلاصة في ستة أسطر

1. **الموقع هو المنتج، والبوت يعالج الصور أيضاً** (قرار المالك 2026-09-06).
   بعد `/start` يختار الزبون: بالبوت أو بالموقع. راجع `ARCHITECTURE.md §5`.
2. **الترميم بنماذج AI** — فلاتر Canvas لا تبلغ نتيجة Remini. الشعار القديم
   «بدون ذكاء اصطناعي» أُلغي.
3. **لا نظام دفع** في هذه المرحلة، ولا نجوم تلگرام.
4. **سقف الدقة 2048px إلزامي** قبل أي نداء للمزوّد — بدونه الفاتورة تتضاعف 15 مرة.
5. **كل نداء للنماذج عبر `lib/enhance/provider.ts`** — لا استدعاء مباشر.
   v1 على `gemini-3.1-flash-image` وحده، ومستوى الجودة قرار سياسة في مسار
   الـAPI لا حقل في عقد المزوّد.
6. **كل مسار يعالج صوراً يمر على `lib/ratelimit.ts`** — صمّام أمان الفاتورة،
   و**يفشل مغلقاً**: بلا عدّاد لا معالجة.

## نطاقك

**بُدِّل التقسيم بقرار المالك (2026-09-04):**

| المجال | المالك |
|---|---|
| الباك-إند والبوت — `app/api/**`, `lib/**`, `telegram-bot/**` | **Claude Code** |
| الواجهة والتصميم — `app/page.tsx`, `app/layout.tsx`, `app/globals.css`, `public/**` | **Codex** |

راجع `docs/ARCHITECTURE.md §6`. لا تعدّل ملفاً خارج نطاقك بدون تنسيق عبر
`docs/REVIEW.md`.

## المشاركة في النقاش — `docs/REVIEW.md`

**النقاش بين الوكلاء يجري في `docs/REVIEW.md`، لا في GitHub Issues.**

السبب: لا يملك أيٌّ من الوكيلين حساب GitHub خاصاً به — كلاهما يكتب بصلاحية
المالك، فلا يميّز أحدهما تعليق الآخر عن تعليق المالك. والقاسم المشترك بينهما هو
**ملفات الريبو** لا واجهة GitHub.

**البروتوكول:**

0. **`npm run channel <اسمك>` قبل أي شيء.** يطبع المهام المفتوحة، وما استجدّ
   لك منذ جولتك الأخيرة، وعنوان جولتك التالية جاهزاً للنسخ. الكتابة بلا تشغيله
   هي سبب كل أعطال هذه القناة: جولة 11 لم توجد قط، وجولة 16 كُتبت مرتين في
   اليوم نفسه، وجولة هبطت بعد جولة أحدث منها لأن كاتبها لم يعلم بوصولها.
1. `git pull` واقرأ من آخر جولة قرأتها.
2. **أضف قسمك في نهاية الملف.** لا تعدّل ما كتبه الوكيل الآخر إطلاقاً.
3. عنوان القسم: `## جولة N — <اسم الوكيل> — <التاريخ>`
4. حدّث قسم «الأسئلة المفتوحة» في أعلى الملف.
5. `commit` ثم `push` على الفرع نفسه.

**عند إبداء رأيك في قرار معماري:**

- **اذكر الملف والسطر** الذي يستند إليه رأيك.
- **اعترض بالأرقام** إذا اعترضت على تقدير تكلفة، لا بالانطباع.
- **إذا خالفت قراراً في `DISCUSSION.md`** فاذكر رقم القسم صراحةً واشرح ما تغيّر.
- القرارات المستبعَدة في `DISCUSSION.md §5` مغلقة بقرار المالك — لا تُعاد.

GitHub Issues يبقى **سجلاً رسمياً للقرارات النهائية فقط**.

## قواعد العمل

- `npm run lint && npm run build` يجب أن يمرّا نظيفين قبل أي دفع.
- لا تُسكِت خطأً لتمرّ البناء — أصلح السبب.
- نصوص المستخدم بالعربية، تعليقات الكود بالإنجليزية.
- الأسرار لا تدخل الگت. `.env.example` يوثّق الأسماء فقط.

## أوامر المشروع

```bash
npm install
npm run dev     # vinext dev
npm run build   # vinext build
npm run lint    # eslint
```
